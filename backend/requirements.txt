fastapi>=0.115,<0.120
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.6
python-multipart>=0.0.12
google-genai>=0.6
pillow>=11.0
numpy>=2.0
httpx>=0.27
python-json-logger>=2.0
pyyaml>=6.0
pytest>=8.3
pytest-asyncio>=0.24
respx>=0.21
pyjwt[crypto]>=2.9
cryptography>=43.0
cbor2>=5.6
# v17 — OTP providers. Aliyun SDK is optional at import time
# (otp.py soft-imports), but we ship it so prod deploys don't 503 on
# first SMS / DirectMail call.
aliyun-python-sdk-core>=2.15
# v17d — distributed rate-limit backend. Optional at runtime
# (rate_buckets falls back to SQLite if REDIS_URL is unset or
# the import fails), but baked into the image so multi-instance
# deploys can flip the env var without a rebuild.
redis>=5.0
